using System;
using System.Linq;
using System.Text;
using System.Collections.Generic;

namespace CodeTest
{
    class UniqueBinarySearchTreesII
    {
        private static void BuildBST(List<TreeNode> bstTs, bool[] flags, TreeNode currentNode, int currentCount, int targetCount)
        {
        }

        static void Main(string[] args)
        {
            const int N = 4;
            bool[] flags = new bool[N];
            Console.WriteLine();
            Console.WriteLine(string.Join(", ", new List<int>()));
        }
    }
}
